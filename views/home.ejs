<!DOCTYPE html>
<html lang="en">

<%- include('./components/head.ejs') %>

<body>

    <%- include('./components/navbar.ejs') %>

    <h1>this is home page</h1>
    <%- '<h1>hello world</h1>' %>
    <% if(blogs.length) { %>
        <% blogs.forEach(blog => { %>
            <h1><%= blog.title %></h1>
            <p><%= blog.intro %></p>
        <% }) %>
    <% } else { %>
        <h3>No Blogs Found.</h3>
    <% } %>
</body>

</html>